spring:
  # stdio
  main:
    web-application-type: none
    banner-mode: off
  ai:
    mcp:
      server:
        name: hive-image-search-mcp-server
        version: 0.0.1
        type: SYNC
        # stdio
        stdio: true

